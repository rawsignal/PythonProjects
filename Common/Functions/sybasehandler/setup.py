from setuptools import setup

setup(name='sybasehandler',
      version='1.0',
      description='The test wrapper is designed to allow for easy test building and execution.',
      author='lrossign',
      license='MIT',
      packages=['sybasehandler'],
      include_package_data=True)
